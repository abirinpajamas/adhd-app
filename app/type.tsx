// screens/types.ts (you can create a new file for this)

// Define a type for your entire navigation stack
export type RootStackParamList = {
  Home: undefined; // 'undefined' means this screen has no parameters
  ManualCalculation: undefined; // This screen also has no parameters
  // You would add other screens here, e.g.:
  // Details: { userId: string };
};
